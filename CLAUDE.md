# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Commands

```bash
npm run dev        # Start dev server (Next.js + Turbopack on port 3000)
npm run build      # Production build
npm run lint       # ESLint
```

> **Windows SSL note:** The dev script runs `cross-env NODE_OPTIONS=--use-system-ca next dev`. This is required because Node.js's built-in fetch (undici) does not trust the Windows system certificate store by default, which causes all Supabase calls to fail with `UNABLE_TO_VERIFY_LEAF_SIGNATURE`. Do not remove `cross-env NODE_OPTIONS=--use-system-ca` from the dev script.

## Stack

- **Next.js 16** (App Router, Turbopack) — `src/app/`. This is a newer major than most training data; APIs and conventions differ. Consult `node_modules/next/dist/docs/` before relying on remembered Next.js behavior (see `AGENTS.md`).
- **Tailwind CSS v4** — no config file; classes are inlined
- **Supabase** — auth + PostgreSQL database
- **@anthropic-ai/sdk** — AI features (`src/lib/claude.ts`, e.g. `parseExpenseFromText` using `claude-haiku-4-5`)
- **lucide-react** for icons (some icons have stale cache issues in dev — swap to a confirmed-working icon if you get module factory errors)
- **date-fns** for date math

## Architecture

### Route groups

```
src/app/
  (auth)/          — login, register, forgot-password (no sidebar)
  (app)/           — all authenticated pages (sidebar + topbar layout)
    dashboard/     — personal finance dashboard
    expenses/      — expense logging
    business/
      dashboard/   — Sales/Warehouse/Logistics ERP dashboard
      finance/     — Finance overview (REAL data computed from Book Keeping + Settings)
        bookkeeping/      — Book Keeping (transactions, debit/credit, full-page Summary/View/Upload)
        settings/         — Finance Settings (Accounts/Banks/Departments/Types of Expense/General)
        reimbursement/    — Reimbursement requests (approve → records to Book Keeping)
        utility-expense/  — Utility Expense requests (approve → records to Book Keeping)
        income-statement/ — P&L connected to Book Keeping + J&T/SPX courier shipping & PAR (real gross profit)
        cards/            — CARDS registry (bank/wallet cards, masked reveal, ad-account links) — TABLE layout
        fb-billing/       — FB Billing: Daily Spend (analytics) + Paid Charges (exact billed → Book Keeping)
        subscriptions/    — Recurring bills (AI tools/SaaS) → auto-posts to Book Keeping as OPEX
      board/       — PROBLEM COST SOLUTION: Problem Management (Root Cause Analysis) + settings/ (departments)
      ecommerce/
        pages/     — Pages & Store management
        roas/      — Page ROAS Tracker (editable→synced Ad Spent, VAT, ROAS = sales/(adspent+vat))
        sales/     — Sales Tracker (+ Tools → Update J&T Shipping Fee Excel import)
        adspent/   — Adspent ROAS Summary
        testing/   — Product Testing (item requests, creatives, 2-level approval, archives, collections)
        bm/        — BM & Ad Account (Facebook BM CRUD + Bm_Add_Template.xlsx upload)
        profitability/ — Profitability Formula (live cascading P&L + 12% VAT, Partners mode, Upgraded matrix)
      ads/         — ADS section: Ads Overview, facebook/ (full dashboard), tiktok/shopee/lazada (coming soon), accounts/ (FB ad-account registration + archive + auto status sync)
      channels/    — SALES CHANNELS: overview + tiktok/shopee/lazada per-channel net (API to follow)
      affiliates/  — AFFILIATES: overview, list (CRUD, editable commission %), sales, leaderboard, commissions
      reports/     — REPORTS: sales, adspend, pnl (multi-channel roll-up, separate from Income Statement)
      logistics/   — LOGISTIC & INVENTORY (LHIKE modules, ALL BUILT except settings/): purchase-order/, supplier/,
                     inventory/ (Inventory Dashboard) + inventory/{products, unit-codes, stocks, transactions},
                     warehouse/ (Warehouse Dashboard) + warehouse/{fulfillment, shipped-out, ppw},
                     rts/, settings/ (placeholder)
      users/       — USER MANAGEMENT (BUILT: roster, roles matrix, enforcement, company logo)
  api/
    auth/          — login, register, forgot-password REST handlers
    pancake/       — Pancake POS proxy: orders (+ orders/create, orders/send-courier, orders/update-status),
                     debug (probes: partners/raw_order/statuses), pages, products (GET + create/ + delete/), geo
    fb/            — Meta (Facebook) Marketing API proxy: insights/ (read incl. ad preview), manage/ (write incl. adrules)
    ph-geo/        — PSGC proxy (provinces/cities/barangays, 24h cache; "NCR" pseudo-province) for cascading address dropdowns
```

### Auth flow

Auth is **cookie-based, not Supabase SSR**. The login API route (`/api/auth/login`) calls `signInWithPassword` and manually sets `sb-access-token` and `sb-refresh-token` cookies. The middleware (`src/middleware.ts`) checks for `sb-access-token` on every non-public route.

- Use `createSupabaseBrowserClient()` (anon key) for auth operations like `signInWithPassword`
- Use `createSupabaseServerClient()` (service role key) for admin operations like `createUser` or direct table writes
- The app layout (`src/app/(app)/layout.tsx`) reads the cookie manually and calls `supabase.auth.setSession()` to rehydrate the client-side session

### Plan gating

Plans are `trial | pro | premium` (defined in `src/lib/types.ts`). Business mode requires `premium`. The `UpgradeModal` component is shown when a locked feature is accessed. `PLAN_LIMITS` and `PLAN_PRICES` in `types.ts` are the source of truth.

### Shared data pattern (Pages & Store)

Pages & Store data flows through a React Context, **not** direct imports:

- `src/lib/pages-store.tsx` — `PagesProvider` (mounted in `src/app/(app)/layout.tsx`) holds the live list in state. Source of truth = Supabase `store_pages`; `localStorage` `pesowise_pages` is a same-session read cache that paints instantly, then gets refreshed.
- Consume with `usePages()` (full CRUD) or `useActivePages()` (non-archived only). The business dashboard and ROAS Tracker both read pages via `useActivePages()` — this is how a store's Pancake credentials reach those modules.
- `src/lib/pages-data.ts` only defines the `PageStore` type and the original seed array (now only used as a first-run fallback).
- ⚠️ **`useActivePages()` returns a FRESH array every render.** Key fetch effects off a value string (e.g. `pagesKey`), never off array identity, or you get an infinite fetch loop.

A page is "connected" to the dashboard/ROAS when it has both `api_key` and (`pancake_page_id` or `shop_id`).

### Pancake POS integration

The business dashboard and ROAS Tracker pull real order data from Pancake POS Open API via the server-side proxy `src/app/api/pancake/orders/route.ts` (keeps credentials server-side, avoids CORS). `src/app/api/pancake/debug/route.ts` is a diagnostic endpoint for inspecting raw Pancake responses.

Non-obvious facts (verified against a live shop — do not "simplify" these away):

- **Base URL / endpoint:** `https://pos.pages.fm/api/v1/shops/{SHOP_ID}/orders`. Auth is the `api_key` query param. The client passes the shop ID as the `page_id` query param to our route.
- **Date filter:** `startDateTime`/`endDateTime` are **Unix timestamps** (not `from_date`/`to_date`), plus `updateStatus=inserted_at`. `toUnix()` converts `YYYY-MM-DD` to a PHT (UTC+8) timestamp. Pagination uses `page_number` + `page_size` (max 100), not `page`.
- **`status` is a number, `status_name` is the string.** Verified primary status codes: `0` new, `1` confirmed, `2` shipped, `3` delivered, `4` returning (For Return), `5` returned, `6` canceled (Unfulfilled), `7` removed (excluded from totals), `8` packaging, `9` waiting-for-pickup, `11` restocking.
- **In Transit / On Delivery are courier sub-statuses, not primary statuses.** They live in `order.partner.partner_status` (`on_the_way` = In Transit, `out_for_delivery` = On Delivery, `undeliverable` = problematic). These can't be filtered server-side, so they're tallied by paginating all orders.
- **Sales = the `cod` field**, matching Pancake's own `aggs.cod.value`. Exact per-status counts/sums come from one-row `filter_status[]` requests reading `total_entries` + `aggs.cod.value` (authoritative, not pagination-limited). The `byDate` breakdown (for daily ROAS) also sums `cod` so it reconciles with the dashboard totals.
- **Dashboard card formulas:** Total RTS = returning + returned; **Unfulfilled = packaging (status 8)** — not canceled; Fulfilled = delivered + shipped + waiting-for-pickup. Cancelled (status 6) is its own separate card.

### UI conventions

- **`DateRangePicker`** (`src/components/business/PancakeDatePicker.tsx`) = the standard date filter for table filter rows (Pancake-style: calendar button → body-portal popover w/ preset rail Today/Yesterday/Last 7·14·30 days/This month/Last month + Start/End inputs + dual-month calendar + Clear/Close/Apply — applies agad, no Search click). Used by: Fulfillment (all date cols), Sales Tracker (`COL_DATE_RANGE`), Book Keeping posted date, Reimbursement date, Utility date + due date (due filter became a RANGE: `dueFrom`/`dueTo` replaced `due`), Purchase Order issue date, Unit Codes date created. Wire pattern: `onApply` sets BOTH draft + applied filter state + `setPage(1)`.
- `fmtPeso(n)` is a **page-local helper** in `src/app/(app)/business/dashboard/page.tsx` only — not exported from utils. Other pages (Sales Tracker, personal dashboard) use `formatCurrency` from `src/lib/utils.ts`. When adding currency display, use `formatCurrency` from utils unless working directly inside the business dashboard page. Never write ₱ via PowerShell string ops (double-encodes UTF-8 and corrupts the symbol) — always write it through the Write tool or Edit tool.
- Card heights: `h-[78px]`, amounts: `text-2xl font-bold`, labels: `text-[11px] uppercase tracking-wider`
- Global CSS classes in `src/app/globals.css`: `.page-title` (page headings), `.scrollbar-dark` (dark sidebar scrollbar)
- The sidebar is `h-screen` with `overflow-y-auto` on the nav — keeps it scrollable without overflowing the viewport
- Stale `.next` cache causes hydration mismatches after large style changes. Fix: stop server → `Remove-Item -Recurse -Force D:\Claude Files\pesowise\.next` → restart + hard refresh browser

### ROAS Tracker specifics

- ROAS color thresholds (`roasColor`, Ads-Manager tiers): `<= 0` gray (`#94a3b8`, no spend), `>= 4.9` green (`#16a34a`), `>= 3` amber (`#d97706`), `>= 2` orange (`#ea580c`), else red (`#dc2626`)
- **Default date range is TODAY→TODAY** (not month-to-date). The FETCH window is deliberately widened to always span yesterday..today so the "Today vs Yesterday" card has data even on a single-day range — the DISPLAY range stays exactly what was picked, so no extra rows appear.
- Tabs (Daily Breakdown / Page Report) sit **directly under the page title**, Facebook-Ads style — not below the filters.
- Page Report ROAS = `sales / adspent` (NO VAT); the Today-vs-Yesterday card = `sales / (adspent × 1.12)` (WITH VAT). Two different formulas on purpose — mirror each exactly.
- Tables only render when pages are explicitly selected — empty state otherwise
- The TOTAL table (aggregate across all selected pages) always renders alongside per-page tables when any page is selected
- Total Amount row: `#1e293b` (dark navy), Average row: `#334155` (dark slate) — keeps ROAS color coding legible

### Sales Tracker specifics (`src/app/(app)/business/ecommerce/sales/page.tsx`)

The Sales Tracker uses `phase=rows` on the `/api/pancake/orders` route. This phase skips aggregates, paginates all orders with `ROW_FIELDS` (slim payload), and returns `{ success, phase:"rows", rows }` where `rows` is the output of `mapOrderRow()` in `route.ts`.

**Critical field mappings** (verified against live API — do not "simplify"):
- **Shipped Out** = `time_send_partner` (top-level field). `partner_inserted_at` is a **filter param** sent to the API, NOT a field returned in order objects — using it as a response field always gives blank.
- **Parcel Status** (courier) ≠ **Order Status** (process). They are separate columns. Parcel Status reads `partner.partner_status` → `PARTNER_STATUS_LABEL` map → latest `extend_update[].status` → "Not Available".
- **Order Status** raw values from Pancake are lowercased non-canonical strings. Normalize via `ORDER_STATUS_ALIAS`: `"submitted"→"Confirmed"`, `"packing"→"Packaging"`, `"canceled"→"Cancelled"`, `"waiting for pickup"→"Waiting for Pick Up"`, etc. `normalizeOrderStatus()` applies the alias map with Title-Case fallback for unknown values.
- **Parcel Update** = max of `last_update_status_at`, `partner.extend_update[].update_at`, fallback `updated_at`.
- **Parcel Qty** = count of **distinct product names** in `items` (`new Set(items.map(name)).size`). Not total units.
- **Courier** = `partner.partner_name` trimmed — never fall back to raw `partner_id` (numeric ID pollutes the dropdown).
- **Retention By** (field key `retention_by`, was `intern_name` — fully renamed everywhere). Reads "retention" tag → `creator.name` → tag editor → `last_editor`.
- **Verifier** / **Upsell By**: blank (not "-") when the order has no matching tag.
- **Shift**: always "Not Available" — Pancake has no shift/staff schedule data.
- **CSR** = `creator.name` (the staff who CREATED/encoded the order in Pancake), fallback `last_editor.name`, else **"System"** (storefront/API orders with no staff encoder). `csr_id` tracks the Pancake user ID. `mapOrderRow` maps this — it was previously unmapped (blank for synced orders).

**CRITICAL — row dates must be Philippine-time (`toPHDate`)**: `encoded_date` / `date_added` (→ `order_date`) / `shipped_out_date` are computed by `toPHDate()` (UTC+8 conversion), NOT a raw `inserted.slice(0,10)`. The server's date-window filter (`toUnix`) is PH-time; if rows kept UTC dates, orders created 12am–8am PH (UTC = previous day) get dropped by the client-side `order_date` filter — the "Today shows fewer orders than Pancake" bug. (The `byDate` aggregation already PH-converts; only the rows path needed this.)

**Price (Upsell) via localStorage baseline** (`localStorage` key `pesowise_order_baseline`):
- Pancake has no price history. While an order has no Upsell tag, snapshot its `total_price` keyed by order UID.
- When Upsell tag appears: `price_upsell = current − baseline`, `price_initial = baseline`, `price_final = current`.
- Orders already upselled before the tracker first loaded have no baseline → Price (Upsell) stays blank. This is unavoidable.
- Retention orders (Retention tag, no Upsell tag) show full amount in Initial/Final and blank Upsell — they are NOT treated as upsell.

**`ROW_FIELDS`** — the curated field list sent as `fields[]` to slim the Pancake payload:
`id, items, shipping_address, bill_full_name, bill_phone_number, total_price, cod, partner_fee, shipping_fee, partner, status, status_name, inserted_at, created_at, partner_inserted_at, bank_payments, updated_at, last_update_status_at, time_send_partner, tags, last_editor, marketer, creator`. If a column goes blank, check this list first.

**Column filter row** drives the Pancake fetch window: `colFilters["order_date__from"]` and `colFilters["order_date__to"]` are passed as `from`/`to` to `fetchPageRows()`. Changing those values triggers a re-fetch. Other column filters (courier, order_status, etc.) filter client-side only.

**Dropdown strategy**: Order Status uses a fixed canonical 9-value list (`ORDER_STATUS_OPTIONS`). Courier, Parcel Status, MOP, Page, Platform are data-derived (unique values from loaded rows). Never swap these — order status raw values are non-canonical and can't be used as the option list.

**`useHoverTip` / `ToggleChip`**: `useHoverTip(tip?)` is a hook that returns `{ref, tip}` for a 2-second-delay tooltip portaled to `<body>`. `ToggleChip` wraps a toggle-column chip with an optional tooltip. Tooltips belong on the **column toggle chips** (the `ToggleChip` components), NOT on table column headers.

**`ORDER_STATUS_BADGE`**: soft-background colored pill badges rendered only in table cells, not in the dropdown. Each status has a distinct `bg-*/text-*` color pair.

**Tags limitation**: Pancake `tags[]` officially only exposes `{id, name}`. Per-tag `editor`/`timestamp` are read defensively (spec is incomplete vs live response). All tag-derived columns (Verifier, Upsell By, Retention By, Reserve Date) are re-derived live on every fetch — not persisted anywhere.

**Stock legend + Orders page filter**: below the toggle chips sits a 🟥 Out of stock / 🟧 Low on stock legend and an "Orders [ALL ▾]" dropdown (filters the table to one page's orders; included in Clear Filters). The Order column cell is wired to a **dormant `stockStatus()` stub** (returns "" until the Inventory/Stocks module exists — then wire it to per-item stock and the red/orange text activates via `TODO(warehouse)` marker).

### Add New Order → creates real Pancake orders

The Sales Tracker's "Add New Order" form CREATES orders directly in Pancake POS via two proxy routes (same `page_id`→shop URL convention + `api_key` query param + `classifyConn` error handling as the orders route):
- **`api/pancake/products/route.ts`** — `GET`, proxies `/shops/{shop}/products`, returns a flattened list of one entry per sellable variation `{product_id, variation_id, display_id, name, price}`. The product picker is page-dependent (loads after a Page is selected; lists only connected pages).
- **`api/pancake/orders/create/route.ts`** — `POST`, proxies `POST /shops/{shop}/orders`. NOT idempotent (no retries on ambiguous failures). No prices sent — Pancake derives totals from the variation; `status: 0` (New).
- **`api/pancake/geo/route.ts`** — `GET`, proxies Pancake's `/geo/provinces?country_code=63` / `/geo/districts` / `/geo/communes` (PH country_code = "63"). Drives **cascading address dropdowns** (Province → City → Barangay); the order's `shipping_address` carries the real `province_id`/`district_id`/`commune_id` + `country_code:"63"`. 1-hour cache.
- Removed Price/Shipping fields from the form (pricing is automatic from the product).

### Send orders to courier → books the real waybill (`api/pancake/orders/send-courier/route.ts`)

Fulfillment's "Send orders to courier" makes SPX/J&T actually generate a waybill. The shape of
this request was **recorded off the POS**, not guessed — hook `XMLHttpRequest` on
`pos.pancake.ph`, do one real Quick update, read the payload. Redo that if Pancake changes.

- **Endpoint is `POST /shops/{shop}/orders/update_multiple_orders`** — a bulk endpoint, not
  `PUT /orders/{id}`. Body is **form-encoded** with `orders[0][...]` keys, not JSON. Reachable on
  the public API with an `api_key` (it answers 400 to an empty payload; `orders/send_to_courier`
  and `orders/bulk_update` both 404 — they don't exist).
- **`partner_id` alone does NOT book anything.** It only writes the courier *name* onto the order.
  The order then shows a courier and moves to Waiting for Pick Up with **no tracking number**, and
  the API still answers success. That was the original bug — the ERP reported "Success" while the
  warehouse had no waybill to print.
- **The switch that books is `is_send_to_{courier}_phi`:**
  - SPX (partner 125): `is_send_to_spx_phi`, `is_free_ship_spx`, `is_spx_post`, `note_spx`,
    `allow_mutual_check_spx`, `allow_try_on_spx`, `pickup_time_spx[0]` (unix ts) + `[1]` (range id)
  - J&T (partner 10): `is_send_to_jnt_phi`, `payment_jnt_phi: "PP_PM"`, `is_drop_off_jnt_phi`,
    `signpart_jnt_phi`, `insurance_services_jnt_phi`
- **Payload is deliberately minimal** (id + partner + status 9 + courier flags). The POS sends ~294
  fields because it holds the whole order client-side; the public API returns only ~106 and leaves
  some blank (`shipping_address`). Echoing that thin copy back would wipe data **if** the endpoint
  replaces rather than merges. It merges — confirmed on a live order — but the route still
  snapshots before/after and aborts loudly if any of `customer_name` / `shipping_address` /
  `order_item` / `final_price` / `contact_no` / `items` goes from filled to empty.
- **Booked is verified, not assumed.** After the write the order is re-read: a real booking has
  `partner.service_partner` (deliver_info / parcel_info / fulfillment_info) and
  `partner.extend_code` (= the tracking number). If both are missing the route returns a failure
  with the tracking blank, so a silent non-booking can never read as success again.

## Finance modules

All built to LHIKE ERP user manuals + reference screenshots. Each is a Supabase-backed store (localStorage = read cache) + page following the same conventions (records selector, per-column filter row, full-page screens via early-return, status colored text/badges, `xlsx-js-style` for real `.xlsx`).

- **Finance Settings** (`src/lib/finance-settings-store.ts` + `business/finance/settings/page.tsx`) — the **single source of finance reference data**: Accounts (3 hardcoded `!Cash Advance`/`!Reimbursement`/`!Utility Expense` — only bank editable; + custom, with voucher/revolving-fund flags/members), Banks, Departments, **Types of Expense** (each carries `type` = Debit/Credit/Not Applicable), + **General** toggles (gate shared-expense / type-request fields). The Accounts tab has a DataTables-style records selector + filter row.
- **Book Keeping** (`src/lib/bookkeeping-store.ts` + `business/finance/bookkeeping/page.tsx`) — transactions list. **Tabs = Accounts**; **Debit/Credit derives from the selected Type of Expense's `type`** (`splitAmount`), NOT a separate account-type. Reads all dropdowns from Finance Settings. Full-page **View Summary** (tabs Type-of-Expense/Banks, collapsible running-balance cards, pie graph), **View Transaction** (form + history + Request Disable→approve workflow, view toggle Enabled/Disabled/**For Approval**), **Upload** (real styled `.xlsx` template + parse; columns POSTED DATE/ACCOUNTS/TRANSACTION/DEPARTMENT/TYPE OF EXPENSE/DEBIT/CREDIT/BANK/VOUCHER). `pushBookkeepingTxn()` lets other modules append a txn straight to localStorage.
- **Reimbursement** (`src/lib/reimbursement-store.ts`) & **Utility Expense** (`src/lib/utility-store.ts`) — dedicated request modules (Payee/Supplier, Requested By, Particular/Description multi-add, Qty/Unit Price/Total, Mode of Payment, auto Control No, Upload). Status pending/approved/**declined**. Full-page View (read-only form + status banner + Export `.xlsx` + HISTORY card) with Approve/Decline. **On approve → records a Book Keeping debit entry** (account `!Reimbursement` / `!Utility Expense`, bank from that Settings account, voucher = control no) via `pushBookkeepingTxn`. Reimbursement adds Shared Expense/Retail/Business-Dev (gated by `hide_shared_expense`); Utility adds Request Form (RFP/HRA), Type of Request/Retail/Business-Dev (gated by `hide_type_request`), control no `PRF-HRAD-{year}-{seq}`.
- **Finance Overview** (`business/finance/page.tsx`) — NO longer mock; computes Gross/Operating Revenue, Adspent/COG/Shipping (from account flags), OPEX+Revolving Fund, and per-bank running balances (Σ credit − debit) from enabled Book Keeping txns in the date range.
- **CARDS** (`business/finance/cards/page.tsx` + `src/lib/cards-store.ts`) — bank/wallet card registry, rendered as a **professional TABLE** (not gradient card mock-ups — user rejected those). Per-row 👁 reveal of card number/account #/CVV (masked by default), provider colour dot, QR view, edit, admin-only delete, provider filter chips, and a linked-ad-account count.
- **FB Billing** (`business/finance/fb-billing/page.tsx`) — two lanes: *Daily Spend* (`fb_billing_records`, analytics only, does NOT post to Book Keeping) and **Paid Charges** (`fb_paid_charges`, the exact VAT-inclusive amount billed to the card). Only the Paid lane posts to Book Keeping (account `Facebook Ads`, type `Facebook Ads Billing`).
- **Subscriptions** (`business/finance/subscriptions/page.tsx` + `src/lib/subscriptions-store.ts`, table **`finance_subscriptions`**) — recurring bills (AI tools/SaaS). EXACT PHP per subscription, no FX. `isDue` uses a catch-up rule (fires once the billing day has arrived or passed this cycle and it hasn't billed yet), `last_billed_period` (`YYYY-MM` / `YYYY`) is the dedup guard. Posts a Debit under the `Subscriptions` account with a `Software Subscription` type that the page auto-creates with `opex: true`. ⚠️ **The table is `finance_subscriptions`, NOT `subscriptions`** — a pre-existing Supabase starter `public.subscriptions` (no `business_id`) made the original migration fail with `42703`.

### Finance adspent model (changed — read before touching Income Statement)

- **Adspent = actual FB Paid charges** (`fb_paid_charges`, filtered by `paid_date` in range), NOT the Meta insights estimate and NOT the manual `is_adspent` bookkeeping debits. The user wanted real money-out only ("sa fb ads lang muna").
- Paid amounts are **VAT-inclusive**, so they are split: `Adspent (ex-VAT) = fbPaid ÷ 1.12`, `VAT = fbPaid − Adspent`.
- **The 12% VAT is OPEX, not a gross-profit deduction.** `Gross Profit = Revenue − Adspent(ex-VAT) − COG − Shipping`; `OPEX = bookkeeping OPEX + VAT`; `Profit & Loss = Gross Profit − OPEX`. Finance Overview shows ADSPENT ex-VAT with a **separate "12% VAT" card**; VAT counts toward *Total OPEX + Revolving* but is **excluded from the Revolving Fund** card.
- **No OPEX double-count:** paid charges post to Book Keeping under the non-default `Facebook Ads Billing` type, which is not OPEX-flagged and whose account has `is_adspent: false`, so those ledger rows only reduce bank funds.
- **Warehouse Inventory** row = `Σ(cog × itemRemaining)` over non-deleted/non-archived items, plus a piece count. It is a current balance (the date picker does NOT apply) and is deliberately kept OUT of Projected Company Funds — stock is not cash.

## Product Testing (`src/lib/product-testing-store.ts` + `business/ecommerce/testing/page.tsx`)

localStorage-backed. Dashboard (records selector, filter row, Status badge + creatives/links count chips, Verdict colored text, New/View/Edit/Archive actions, View Archives + View Collections). **2-step New/Edit form** ("ADD ITEM REQUEST" → "ITEM CREATIVES"): RDP Control Number (auto `RDP-{year}-{seq}`), product fields, COG + **Get Percentage** (cog/srp×100), SRP for 1pc, **creatives** (name + multiple links), platform/status/verdict. **Archive needs a Yes/No confirm**. **View ("ITEM REQUEST")** = bordered key|value table + **two-level admin approval** (Level 1 / Level 2 cards, Approve/Decline with remarks) + History box + Edit Item/Close. **Delete = request→approval** (red Request to Delete in Edit → admin Approve removes / Decline keeps). **Images stored as downscaled data URLs** (`fileToImageData`, ≤700px) so the actual `<img>` renders, not the filename.

## Multi-channel Ads & Facebook (Meta) integration

New business sidebar sections (Sidebar active-link uses a **longest-match `bestHref`** — don't revert to bare `startsWith`, it lights up parent routes): **ADS · SALES CHANNELS · AFFILIATES · REPORTS** (+ E-COMMERCE). Shared **`DateRangeFilter`** (`src/components/business/DateRangeFilter.tsx`, default "This month", incl. "Maximum" = last 37 months) on every ADS + SALES CHANNELS page except Ad Accounts.

**Facebook proxy** — token kept server-side (passed as query param like Pancake), 5-min in-memory cache:
- **`api/fb/insights/route.ts`** (read) modes: `list` (ad accounts on a token), `meta` (account_status for auto-sync), default `account_id&from&to` (daily account spend → adspent sync), **`rich`** (level-aware: `level=campaign|adset|ad` + `parent`; returns rich per-object metrics + budgets + bid strategy + dates), `trend` (daily spend+sales), `tree=adsets|ads`. Adds `action_attribution_windows=["7d_click","1d_view"]` to **match Ads Manager exactly**. Metrics use **OMNI** action types (website + Meta) + native `purchase_roas` field. ABO campaign budget falls back to summed active ad-set budgets.
- **`api/fb/manage/route.ts`** (write) actions: `status` (pause/activate=publish), `delete`, `update` (rename/daily_budget), `duplicate` (deep copy, PAUSED), `create_campaign` (PAUSED). **Created/duplicated objects default PAUSED** — real money guardrail.

**Ad Accounts** (`ads/accounts/page.tsx` + `src/lib/fb-store.ts`, localStorage `pesowise_fb_accounts`) — Pages-&-Store-style CRUD registration: Name, Owner (searchable combobox), Platform, **Focus**, Ad Account ID, API Token, Mapped Page, status, Archive toggle (Page URL/Currency fields removed; currency defaults PHP). **Status auto-syncs from FB** `account_status` on load. **Default API Token card** (`useFBDefaultToken`, key `pesowise_fb_default_token`): ONE shared token auto-filled (locked, with custom-override link) into every registration, `setAllTokens()` applies to all accounts, **60-day countdown badge** ("N days left" + "Renew by date") and an amber/red expiry banner above the search bar in the last 3 days / after expiry; Renew modal re-applies everywhere.

**Facebook Ads dashboard** (`ads/facebook/page.tsx`) — 3 tabs (active tab persists on REFRESH only; in-app navigation back resets to Dashboard via a module-level flag + `performance.navigation` type check):
- **Dashboard** — overview cards react to Account/Owner/Objective/Search but **NOT Status** (Status narrows the table only; options All/Active/Paused/**With spend** [default] — Paused = paused AND spent). **Ad Budget card = active OR spent campaigns**. **Campaign Performance is a view-only mini Ads Manager**: 3 level tabs (Campaigns/Ad Sets/Ads, lazy-loaded per level) with Meta-style checkbox selection cross-filtering (select campaigns → Ad Sets/Ads filter; "N selected ✕" tab badges w/ cascade clear; name-click drills), ad thumbnails, per-level budget labels ("Daily"/"Lifetime"/"Ad set budgets"/"Using campaign budget"), sortable columns (shared `CONV_COLS`/`MSG_COLS` + `AD_EXTRA_COLS` on Ads: Link Clicks, CTR(all), Video Plays 100%, Quality/Engagement Ranking), gridlines, TOTAL footer (`computeTotal`).
- **Daily Ad Spend** — per-account daily w/ Account/Owner/Status filters + Owner column.
- **Ads Manager** — Meta-style connected panels (Campaigns·Ad Sets·Ads with "N selected ✕" badges, cross-filtering, default "All ad accounts" incl. per-row account token resolution). Filter bar order: **Manage Ad Account → Owner** → Objective → Status. **Status toggles publish INSTANTLY** (no draft): `setStatusNow` = optimistic flip + spinner inside the toggle knob (`togglingIds`) → `/api/fb/manage` status → `load(true)` → green "1 X updated" toast; on failure revert + ⚠ flash. Bulk Turn on/off (`bulkSetStatus`) = immediate sequential apply with the action-bar progress bar → toast. **Draft/publish workflow = BUDGET EDITS ONLY**: inline budget edits queue as DRAFTS (amber label) → "Review and publish (N)" modal (per-row Change/Errors; failures keep drafts) → progress bar fills NEXT TO "Discard drafts" in the action bar → bottom-right green "N updated" toast + View details. Discard has a confirm modal. Budget editable only where owned (CBO campaign / ABO ad set); inline "Publish" applies one row immediately. **Automated rules (More ▾ in the action bar)**: "Create a new rule" → chooser modal (auto-apply card + Custom rule radio) → full Meta-style custom-rule form (Rule name, Apply rule to [All active campaigns/ad sets/ads or N selected], Action [Turn off/on, Send notification only, Increase/Decrease daily budget by % or ₱ + cap], Conditions rows [metric/operator/value, Add/✕, "…" = attribution-window popover], Time range [37 months Maximum→LIFETIME…], Schedule [Continuously=SEMI_HOURLY / Daily / Custom days+times], Notification On-Facebook locked check, Subscriber chip prefilled from `/me`); "Apply an existing rule" → picker modal that merges the checked rows' ids into the chosen rule's `<entity>.id` filter via `rule_update` (eligibility: rule's entity_type must match the current level AND rule's account must own the selected rows; amber warnings when an all-active rule gets narrowed or selection spans accounts); "Manage rules" → in-app modal listing `adrules_library` rules across accounts w/ enable-disable toggle + delete (confirm). Rules are REAL — created via `api/fb/manage` actions `rules_list`/`rule_create`/`rule_status`/`rule_delete`/`me` → Meta `act_X/adrules_library`, so they run on FB's side every 30-60 min. ⚠ Currency thresholds & change_spec amounts/limits sent in CENTAVOS (×100, like budgets); campaign budget action = `CHANGE_CAMPAIGN_BUDGET`, ad-set = `CHANGE_BUDGET`; ads have no budget actions. **Ad Preview** (Ads level, next to Turn off): Graph `/{ad}/previews` iframe modal w/ placement switcher + "X of N ads" picker. **No delete anywhere.** Frozen left columns (checkbox+On+name) use fixed widths + 1px overlap + z-layers (thead z-30 > body sticky z-10) — don't "simplify", it prevents seams/bleed-through on scroll.
- **Perf/rate limits:** lazy per-level loads + 5-min server cache (`nocache` only after edits). Fetching all levels×accounts with nocache triggers FB **#17 "too many API calls"** (account-level, ~15-60min cooldown) which silently blanks `effective_status`/parent ids from the meta edge — parent ids are ALSO read from insights fields as fallback so cross-filtering survives throttling. Toggles use the object's own **configured `status`** (not `effective_status` — children of paused parents would always look off) and update optimistically.
- Auto-syncs each mapped account's summed daily spend → `useAdspent` store → ROAS Tracker + Income Statement. The **ROAS Tracker also self-syncs FB spend on Submit** for its exact submitted range (background `syncAdspentFromFB`, keyed by ad-account `page_name` → page), so any month's Ad Spent shows without opening this dashboard. Dashboard pulls from ALL credentialed non-archived accounts (not just status "Active").

**Adspent store** (`src/lib/adspent-store.ts`, Supabase `adspent_entries`, keyed `${pageId}|${date}`) — per-page-per-day ad spend, `setMany` for batched FB sync. The ROAS Tracker + Adspent Summary read it; Ad Spent there is read-only (FB-synced). **VAT lives only in ROAS Tracker / Income Statement / Profitability — the FB Ads dashboard mirrors raw Ads Manager numbers (no VAT).**

> ⚠️ **Finance adspent no longer uses this store.** Income Statement + Finance Overview source ad spend from **actual FB Paid charges** (`fb_paid_charges` — what was really billed to the card), not the Meta insights estimate, and manual `is_adspent` bookkeeping is deliberately excluded ("FB paid lang"). See the Finance section below.

**Facebook Ads dashboard + Ads Manager default to TODAY→TODAY** (one shared range state covers Dashboard / Ads Manager / Daily Ad Spend). **Custom rules: a blank condition value no longer blocks Create** — incomplete condition rows are filtered OUT of the payload rather than shipped as `> 0`. Meta may still reject a rule with zero metric conditions; the error surfaces from its API.

Stores: `fb-store.ts`, `channels-store.ts`, `affiliates-store.ts`, `adspent-store.ts`. Components: `DateRangeFilter.tsx`, `ComingSoon.tsx`, `ChannelNet.tsx`.

> **FB tokens — hard-won facts:** the active token is a ~60-day USER token (app "ODM Ad spents", issued Jun 30 2026 → expires Aug 29 2026; `debug_token` endpoint reads expiry). The previous **"Conversions API System User" token could READ but not WRITE ads** — ad-level status changes fail `#100/2859024` ("business admin must accept the non-discrimination policy"), because that certification attaches to the token's IDENTITY and a CAPI system user can't accept it (Meta forum-confirmed dead end). The policy is **per Business Manager** (user has 3: Lumyra Eye Care ×2 + Vertigo Wellness+) and only gates AD-level writes — campaigns/ad sets still toggle. `api/fb/manage` surfaces `error_user_msg`/subcode/blamed field instead of generic "Invalid parameter". "#200 API access blocked" = temporary app-level anti-abuse block from bursty debugging; wait it out. For production use a never-expiring **System User token** (needs the same certification). Treat any pasted token as exposed — advise rotating.

## Logistic & Inventory (LHIKE modules, Supabase-backed)

Sidebar section renamed **LOGISTIC & INVENTORY**; the sidebar now supports collapsible sub-menus via `NavItem.children` (Inventory → Product Items / Stocks / Unit Codes; `bestHref` flatMaps children). All modules follow the LHIKE manuals + reference screenshots, PesoWise-styled.

- **Purchase Order** (`logistics/purchase-order/page.tsx` + `src/lib/po-store.ts`, keys `pesowise_purchase_orders` / `pesowise_po_settings`) — LHIKE list (filter row applied via Search button, Delivered toggle, pagination "Showing X to Y of Z"). **Create Request Order** form: auto-locked numbers `DN-n/CPO-n/CN-n` (`nextPONumbers` = max numeric suffix + 1), Delivery to/Address, Order lines (Supplier picker ← Supplier store, Qty, Item picker ← Product Items, Unit Cost), Delivery Fee (in grand total), Assigned to (max 4, comma-split), Picked-up Date. Submit → **ORDER view** (Export `.xlsx`, status dropdown, HISTORY sidebar). **Status flow** `PO_NEXT_STATUS`: Draft→For Approval→Approved→Paid→Partial Delivery→Delivered (+Cancelled); changes go through a Change Status modal (Remarks* + Assigned user* from `MEMBERS`) and append to `history`. **`poLocked`** (Partial Delivery/Delivered = "beyond Paid") disables Edit/Delete. Row actions: view/edit/delete/**copy order** (prefilled add form, fresh numbers)/**copy link** (`?view=<id>` deep link opens the order). **PO Settings screen** (Settings button, NOT the logistics settings route): Delivery Fee / COG Fee blocks each mapping Account·Bank·Type of Expense·Department from Finance Settings; locked until "Edit PO Settings" (teal). Stock-Out Items = modal stub until Inventory stock lands.
- **Supplier** (`logistics/supplier/page.tsx` + `src/lib/supplier-store.ts`, `pesowise_suppliers`) — Add New (direct button) + Archives toggle; table w/ address "more/less" expander, filter row + Search. Form: contacts, **PSGC cascade** Province (select) → City → Brgy via `/api/ph-geo` (edit mode resolves saved names→codes; ComboInput free-text fallback on PSGC failure), Supplier URL, multi **Item** rows. `activeSuppliers` feeds the PO supplier picker.
- **Product Items** (`logistics/inventory/products/page.tsx` + `src/lib/product-items-store.ts`, `pesowise_product_items`) — columns SKU/Name/COG/Color/Size/Type/Status/Goods/Damage/Loss/**Remaining** (= goods−damage−loss, `itemRemaining`, red when ≤0)/Supplier; EVERY column has a filter box (numeric ones match raw digits). Footer totals: **Total Remaining Qty** + **Total COG Amount** (Σ COG×Remaining, filtered). Tools ▾: Add New (reference order: Name→SKU→Description→COG→…→**Quantity**→Supplier→**Upload Item Picture** (downscaled data URL)→"Is Active?"; Damage/Loss edit-mode only), **Upload Item** (full screen; template **`Add_Product_template.xlsx`** headers `Item Code */Name */Description/COG */Color/Size/Type/Qty */Damage` with TEAL styled header row — export uses the same style; parser is header-tolerant incl. legacy names), Export to Excel, **View All Deleted Items** (delete is SOFT → restore/purge there). Archive = separate flag. Feeds PO item picker + Unit Codes.
- **Unit Codes** (`logistics/inventory/unit-codes/page.tsx` + `src/lib/unit-codes-store.ts`, `pesowise_unit_codes`) — the sales↔inventory bridge: a code = sellable POS product mapping to product item(s)+qty (bundle recipe). Form: **SKU auto-locked** (`nextUnitSKU` UC-n), Unit Code (max 30 + note), Item rows (Qty* + readonly COG from Product Items + picker), Selling Price + readonly **Total COG Amount** (Σ qty×cog), **"Add POS code for Upsell Item"** (also creates `UPS-`-prefixed code — use `addMany` for the pair, two `addCode` calls would clobber state), **Pages with Pancake API Key** = `PageTagSelect` chips of CONNECTED pages + "Will also be added to Pages: …" helper. **Submit auto-creates the product on Pancake POS** via `api/pancake/products/create` (POST `/shops/{shop}/products`; **name/custom_id/barcode ALL = the unit code** (user spec: POS Product name + Product ID = code), retail_price=selling, original_price=totalCog; targets selected pages or ALL connected when none; proxy surfaces Pancake's raw error body and auto-retries a `{product:…}`-wrapped payload on 400/422). **LIVE-VERIFIED Jul 8 2026** (FurFresh PH, shop 1942219430): create succeeds and returns the full product object (variation display_id = the custom_id — that's what Pancake's Product ID column shows). The old bare "HTTP 400" was actually **"Custom Product ID already existes in the system"** — custom_id must be unique account-wide, so duplicate pushes fail (now surfaced clearly). Note: the products GET proxy caches ~5 min, so a just-created product may lag in OUR list endpoint; it's immediate in Pancake's UI. Item picker shows **"SKU - Name"** labels (ComboInput `labels` map; stored value stays the plain NAME — cog/remaining lookups + Pancake push are name-keyed). **View screen = LHIKE "VIEW ORDER"**: blue not-removable-on-Pancake banner, readonly fields (SKU / Unit Code + 30-char note / Item rows Qty+COG+name / Selling Price / Total COG / pages bullet list), **generated CODE128 barcode** of the unit code (**`jsbarcode`** dependency — svg ref + `JsBarcode(el, code, {format:"CODE128"})` in useEffect), Edit/Cancel, **HISTORY sidebar** (Added By = `UnitCode.created_by`, stamped from `currentUserName()` in addCode/addMany; Added Date; Remaining). Table: live Remaining + Total Amount (=totalCog) from Product Items; row checkboxes + bulk archive; **Delete exists** (row trash + bulk Delete → red confirm modal; `deleteMany` in store) and **also deletes the matching Pancake product**: background `deleteFromPancake` → `api/pancake/products/delete` (fresh product lookup, match name/custom_id = code case-insensitive, then `DELETE /shops/{shop}/products/{id}` — live-verified Jul 8; POST/PUT `is_removed` fallbacks kept for other deployments); copy = prefilled add w/ blank code. Tools: Add/Upload (`Add_UnitCodes_template.xlsx`, comma-sep Items/Pages)/Export Excel/**Export PDF** (print-window). Legacy records auto-migrate (string items→{name,qty:1}, amount→selling_price).
- **Stocks (BUILT, live-verified Jul 8)** (`logistics/inventory/stocks/page.tsx` + `src/lib/stock-releases-store.ts` `pesowise_stock_releases`) — LHIKE "STOCKS UPDATE": orange "Choose one category only" + 4 `SearchSelect` dropdowns (inline search box): **Sku Code** = unit-code SKU, **Unit Code** = code, **Item Code** = product-item SKU, **Item Name**; picking one clears the rest. Search → table No/Item Code/Item Name/Quantity Required/Available Qty/**Release Qty** (input)/**Remaining Qty** (readonly preview = Available − Release×Required, red if negative). Unit-code searches list the recipe items (Required = qty per bundle); item searches = single row Required 1. Submit → `releaseStock` adds Release×Required to each item's **`released`** field (ProductItem gained `released`; `itemRemaining` = goods−damage−loss−released, so Product Items' Remaining drops instantly) + logs to stock-releases store; Clear resets. **Sales Tracker stock colors are LIVE**: `stockStatusOf` (was dormant stub — also fixed the dead `col === "order"` check → `order_item`) matches order text against unit codes, bundles = min(floor(remaining/qty)); ≤0 red "out", ≤10 (`LOW_STOCK_BUNDLES`) orange "low".
- **Warehouse is a collapsible submenu** (LHIKE-style, like Inventory): **Fulfillment / Shipped Out (Barcode) / PPW (Barcode)** → `logistics/warehouse/{fulfillment,shipped-out,ppw}`. Parent `/warehouse` page still exists.
- **Fulfillment (BUILT, LHIKE manual)** (`warehouse/fulfillment/page.tsx`) — warehouse view of LIVE Pancake orders (same `phase=rows` proxy + per-page fetch as Sales Tracker, all connected pages merged, DateRangeFilter default This month). **Toggle-column chips** (23 LHIKE columns: Order Date…Error Message; default 7 on), per-column **filter row** (date ranges / selects / text + Search), **Group** dropdown (by page), records selector, pagination, **Total Price** footer. Row actions: 🔍 View Order screen, 👤 **Assign Packer** — LHIKE-style "Assigned To:" searchable dropdown of ERP accounts (Finance Settings `MEMBERS` + `currentUserName()`; swap the source to the real users table when the Users module lands). Header: Refresh, **Export Pick List** (replaces plain Export; checked rows → those only, none checked → ALL records shown; xlsx w/ title+meta block, **PICK SUMMARY** consolidated shelf-walk table [NO./ITEM CODE/ITEM NAME/TOTAL QTY/PICKED ✓ — order items exploded through unit-code recipes so "1x Lumyrax2" becomes 2 pcs Lumyra; unmapped products flagged], **ORDER BREAKDOWN** per parcel [date/customer/page/order/items-to-pick/courier/tracking/PACKED ✓], Picked By / Checked By / Date signature row; teal headers), **Send orders to courier** (replaced Update Tracking Details; truck icon + dropdown To SPX Phi → To J&T Phi, SPX first): Pancake-replica settings modal (left = selected orders w/ search + status badges; right = SPX [Shop pays shipping ✓ default/post office/pick-up shift/voucher/parcel item type Fragile/dimensions/printing note] or J&T [drop-off/partial delivery/price declaration ✓/payment method/max weight/extra note/pick-up time] + Address options toggle + Add action) → Update runs per-order via **`api/pancake/orders/send-courier`** (PUT `/shops/{shop}/orders/{id}` `{partner:{partner_id,…}, status: 9}` — **status 9 "pending" = Waiting for Pick Up** is set in the SAME update so the order leaves Packaging, + a follow-up `{status:9}` PUT if Pancake kept the old status; ⚠ **11 "waitting" is NOT waiting-for-pickup — it's "Restocking"** in the POS UI (user caught this live: sending 11 showed Restocking in Pancake); wrapped/POST fallbacks, raw error surfacing; partner ids from `GET /shops/{id}/partners`: **SPX Phi=125, J&T Philippines=10**) → Pancake-replica progress modal (animated green bar %, Success/Failed cards, Export orders error xlsx, Copy success order ID, All/Success/Error tabs, per-order lines, Back → refresh). **Send-courier LIVE-VERIFIED (user, Jul 9 2026)**: real J&T send from the ERP succeeded — courier assigned + status moved to Waiting for Pick Up on BOTH sides (ERP table and Pancake POS). Only shop-pays/drop-off/note flags are transmitted; other settings are UI-parity (Pancake-side defaults apply). **Parcel Status column = Pancake ORDER STATUS labels** (`orderStatusLabel`, live-verified status_names: 0 new/1 submitted→Confirmed/8 packing→Packaging/11 waitting→Waiting for Pick Up/2 shipped/6 canceled/3 delivered/5 returned) w/ fixed dropdown (All/New/Restocking/Prioritize Order/Confirmed/Packaging/Waiting for Pick Up/Shipped/Delivered/Returning/Returned/Cancelled/Deleted). Debug route has probes: `?probe=partners|raw_order|statuses`. **Tools ▾** → Update Parcel Status (**REAL Pancake update** via `api/pancake/orders/update-status` PUT `{status: N}` per checked row — options = the new ORDER_STATUSES with verified codes only [New 0/Confirmed 1/Packaging 8/**Waiting for Pick Up 9 ("pending")**/**Restocking 11 ("waitting")**/Shipped 2/Delivered 3/Returning 4/Returned 5/Cancelled 6; Prioritize/Deleted are filter-only, codes unknown]; badge preview in modal; refreshes after) + **View COG Sold** (modal: parses `order_item` "Nx NAME" segments → COG from unit-code recipe Σ(item COG×qty) or direct product item; filters Order date/Product/Delivery status; totals). Warehouse-only fields live in localStorage **`pesowise_fulfillment_meta`** keyed by Pancake order id (packer/shift/packed_date/rts_tracking/status+tracking overrides/work_status/remarks) — Pancake has no packer concept; overrides are local notes. NOT built: Export-to-API + Ninjavan ODZ (LHIKE features for couriers we don't use). **Order Date = ONE compact control** (user iterated to this): a calendar-icon button in the Order Date filter cell → **portal popover** (Pancake-style, portaled to body so the table's overflow can't clip it): preset rail (Today/Yesterday/Last 7/14/30 days/This month/Last month) + Start/End date inputs + **dual-month calendar** (range click: start → end, blue range highlight, today ring) + Close/**Apply** (applies + refetches agad). It drives the fetch window (blank = current month PH). Frozen left columns: checkbox (left-0) + No (left-[36px]) sticky w/ fixed widths + rowBg, z-30 header / z-10 body — Sales-Tracker-style. **Sticky header on vertical scroll too**: table container = `overflow-auto max-h-[68vh]`, `<thead className="sticky top-0 z-40">` (titles + filter row stick together; every thead cell needs a solid bg — bg-slate-50 titles / bg-white filter row — or body rows bleed through). 🔍 opens the full **VIEW ORDER screen** (LHIKE): readonly Pancake fields + editable Remarks (seeded from Pancake `note`)/Tracking No./RTS Tracking (gold "Update Tracking Details" saves locally), **Owned / In progress / Done** work-status buttons, **HISTORY** sidebar (Created By/Date, Last Edit By/Date, ENCODED — from `inserted_time`/`last_editor_name`/`last_edit_time`, new `mapOrderRow` fields via `toPHDateTime`; `note` added to ROW_FIELDS). **Last Edit By = the logged-in PesoWise user** for local edits: the app layout persists the Supabase profile to localStorage `pesowise_current_user` → `src/lib/current-user.ts` `currentUserName()` → every Fulfillment meta write goes through `stamp()` (`last_edit_by`/`last_edit_at`, shown with a "via PesoWise" tag; falls back to Pancake's last_editor when no local edit). Tracking numbers confirmed live for BOTH J&T (`JT00…`) and SPX (`SPEPH…`); blank tracking = order not yet handed to a courier. ⚠ `useActivePages()` returns a FRESH array every render — key fetch effects off value strings (e.g. pagesKey), NOT array identity, or you get an infinite fetch loop (the Jul 9 Fulfillment "glitch").
- **Transaction History (Inventory submenu, BUILT)** (`inventory/transactions/page.tsx`) — reads `pesowise_stock_releases`: Date/Type (Stock Release · category)/Reference/Items (release × required = deducted)/Total Deducted + search + pagination + TOTAL footer. Sidebar Inventory children order now: Product Items / Unit Codes / Stocks / Transaction History (LHIKE order).
- **RTS Items (BUILT, LHIKE RTS-ITEMS manual)** (`logistics/rts/page.tsx` + `src/lib/rts-store.ts`, `pesowise_rts_meta` keyed by TRACKING NUMBER) — live Pancake **Returning/Returned** orders across all connected pages (`isRts` on order_status/parcel_status; Pancake date picker = fetch window, default this month). Header: Refresh + **UPLOAD CLAIMS** (blue; **full SCREEN**, LHIKE layout — Template link + Upload Excel File* + Upload/Back; template `Upload_Claims_template.xlsx` = **TRACKING NUMBER + DATE OF CLAIM YYYY-MM-DD**, BLUE 2E75B6 header — walang amount column: **claim amount = the parcel's price** from the loaded RTS row; date parser accepts typed YYYY-MM-DD, Excel Date cells, and raw serials → becomes `claimed_at`; warns kung wala sa loaded list) + **SCAN & RECEIVE** (teal) + **SCAN & CHECK** (red) — scan modals are barcode-scanner friendly (autofocus, Enter submits, running session log + Total count, duplicate-scan rejected w/ timestamp). State lifecycle per tracking: **Unreceived → Received → Checked** (`received_at/by`, `checked_at/by` stamped from `currentUserName()`); badges amber/blue/emerald w/ stamp shown in the RTS Status column. **Actions 🔍 = full VIEW RTS screen** (LHIKE): readonly order info (Order Date/User/Customer/Address/Prov/City/Brgy/Contact/Parcel Qty/Price/Page/Tracking/Shipping Fee/Courier) + editable: **per-item Goods/Damage/Loss counts** (teal/red/red inputs, seeded from parsed order_item), **Reason** radios Buyer/Courier + remarks, **Upload Image** (multi-file → 720px JPEG data URLs, thumbnails w/ hover-✕, "No RTS Image Available" red link when empty), **Status** radios For Approval/Pending, Remarks textarea, **RTS Fee** input → Submit = `saveDetails` (stamps `requested_by/at` on first save + `updated_by/at` every save; **Damage/Loss > 0 auto-tags For Claim**); HISTORY sidebar (RTS requested by/Date, Remarks, Claimed date, Uploaded by/date). **Claim lane**: rose **For Claim** badge → Upload Claims amount → purple **"Claimed · ₱x"** + `claimed_at` (`claimStateOf`). Status filter includes For Claim + Claimed. Footer totals: Price / Shipping Fee / Unreceived / **Received** / Claimed (Unreceived+Received = Price). **CAMERA SCAN (mobile)**: full-screen scanner (`CameraScanScreen`, **`@zxing/browser`** dep, dynamic import, `decodeFromConstraints facingMode:environment`; camera starts on TAP — user gesture unlocks permission + WebAudio). Mode toggle laging kita (SCAN & RECEIVE teal / SCAN & CHECK red). RECEIVE = fully automatic scan→update; CHECK = bottom sheet w/ per-item Goods/Damage/Loss **steppers** (goods defaults to qty) + note + **big always-visible CONFIRM** (h-14) → markChecked + saveDetails (damage/loss>0 auto-For-Claim). **WebAudio beeps** (`beep()`: receive = rising double, check = triple, error = low square buzz) — magkaiba per mode; **duplicate scan → error sound + "ALREADY SCANNED/CHECKED" banner** w/ when+who. Camera-frame cooldown 1.5s per code; manual/hardware input bypasses the guard (`handleDecode(code, true)`). Manual input fallback sa scanner + session counters. **Scan Summary** sa RTS page: ONE date-preset select (Today default/Yesterday/Last 7·30 days/This month/All time) filtering 4 **CLICKABLE** counters — **Received** (# parcels, received_at), **Claims** (# + ₱ total, claimed_at), **Damage** & **Loss** (pcs, summed from checked parcels' item counts, checked_at basis). Clicking a card opens a detail modal: DATE / TRACKING NUMBER / CUSTOMER (+ Received By | Amount w/ ₱ TOTAL | PCS + items breakdown "1× Lumyra") sorted latest-first w/ TOTAL footer. **5th card: EXPECTED RTS** (indigo, FIRST in the row) = lahat ng Pancake-declared returns sa TABLE's date window (hindi statRange) w/ "X received · Y pending" sub-line — the received-vs-expected matcher; click → detail w/ COURIER + STATUS badge (Received ✓ teal / Pending amber), pending-first sort, TOTAL "X received · Y pending" footer (`expectedStats` from rows × meta.received_at). **Manual SCAN & RECEIVE / SCAN & CHECK buttons REMOVED** (user: Pancake-integrated na tayo, camera scan na lang) — ang CAMERA SCAN screen na lang ang scanning entry point (may manual/hardware input fallback naman sa loob nito); ScanModal component deleted. ⚠ **rts-store writes are FUNCTIONAL setMeta only** — back-to-back markChecked+saveDetails on a stale snapshot silently dropped the first write (caught live, fixed via `update(fn)`). ⚠ Phone camera needs a **secure origin** (HTTPS or localhost) — sa LAN IP (http://192.168.…:3000) hindi bubukas ang getUserMedia; manual/hardware-scan fallback pa rin ang gagana doon hanggang may HTTPS/deploy. Filter row (DateRangePicker + text filters + Courier/Status selects), sticky header + max-h scroll, address more/less expander, pagination; footer totals: **Total Price / Total Shipping Fee / Total Unreceived** (price of parcels not yet received) **/ Total Claimed**. Restock-to-inventory from received RTS = future (tie to Stocks releases as stock-in).
- **Shipped Out (BUILT)** (`warehouse/shipped-out/page.tsx` + `src/lib/shipped-out-store.ts`, migrations `0015`/`0016`) — scan a waybill (camera via native `BarcodeDetector` with a `@zxing/browser` fallback, hardware scanner, or typed) → look the order up in the loaded Pancake rows → explode through unit-code recipes → **automatically deduct inventory**. The deduction hits `product_items.released` AND is logged to `stock_releases` with category `"Shipped Out"`, so it shows in Transaction History and counts as the ERP side of the stock-out audit. **Inventory can now be reduced two ways: manually via Stocks, or by scanning here — never do both for one parcel.**
  - ⚠️ **Ordering is load-bearing:** `addScan` inserts FIRST against `UNIQUE (business_id, tracking_no)`, and the deduction only runs when that insert returns `"added"`. A repeat scan beeps ALREADY SCANNED and **never double-deducts**, even from another device.
  - Orders whose item names match no unit code are still logged, flagged "walang na-less".
  - Tabs: *Scan* (LHIKE layout — TRACKING NO + Submit left, an "As of today" panel right with fixed SPX/J&T tallies) and *Shipped Out Report* (date range, courier filter, per-scan table with item names + amount, totals row, Excel export). `amount` is captured **at scan time** (`0016`) — scans recorded before that column existed render "—".
- **PPW — Pending Printed Waybill (BUILT)** (`warehouse/ppw/page.tsx`) — modelled on the warehouse's manual "FB PPW (NEW)" gsheet (4 rows/day: J&T in, SPX in, J&T out, SPX out, against a running balance that closes at zero). Their notes: *PPW* = printed waybill still unshipped (incl. returned), *PFF* = pouched + labelled but the courier didn't collect, *FP* = fulfilled summary, *OFF* = PPW+FP+PFF.
  - **Derived, so nothing is keyed in daily:** IN = the order has a `tracking_no`; OUT = it reached Shipped; **PPW = has tracking AND status NOT in {Shipped, Delivered, Returning, Returned, Cancelled, Deleted}**.
  - *PPW Monitor* tab: total + per-courier cards with amounts, an aging strip (today / 1–2 days / **3+ stale**), the parcel list, Excel export. It is a **live 90-day snapshot**, deliberately NOT bound to the FP date filter, so stragglers can't slip out of view.
  - *Fulfilled Parcel Report (FP)* tab: per-courier counts/amounts for a date range on the **`shipped_out` basis**, a day-by-day SPX/J&T/Total breakdown, Excel export, and a **scan cross-check bar** showing how many fulfilled parcels have a matching Shipped Out scan.
  - ⚠️ The PPW card on the Warehouse Dashboard is **range-scoped** while this page is a 90-day snapshot — the two can legitimately disagree. This page is authoritative.
- **Warehouse Dashboard (BUILT)** (`warehouse/page.tsx`) — six pipeline cards using the **actual Pancake POS colours** (New cyan, Confirmed blue, Restocking (on hold) gold, Packaging purple, Waiting for Pick Up magenta, Shipped orange) + an ops row (Total Orders, PPW, Delivered, Scanned Out Today, RTS, Cancelled). Every card reads `COUNT` over `LABEL (₱amount)`, matching the Sales Warehouse Logistics dashboard. Also: a **Pipeline Summary per Page** matrix (page × status, row/column totals, all-zero status columns hidden but never a column with data, sticky page column, Excel export), graphs **hidden by default** behind a Show/Hide toggle, a packer leaderboard, and a backlog list.
  - ⚠️ There is **no "TO FULFILL" card** — it used to lump New/Confirmed/Restocking together, but the real to-fulfill queue is **Packaging**, now labelled as such.
- **Inventory Dashboard (BUILT)** (`src/components/business/inventory/InventoryDashboard.tsx`) — reachable **two ways from one component**: the standalone `/logistics/inventory` route and a tab inside Product Items. Stat cards (Σ cog×remaining value, remaining qty, active items, damage+loss write-off, released units, low/out counts — low = remaining ≤ 20% of goods), graphs **hidden by default**, a Low Stock table with a days-of-stock-left estimate from the last 30 days of release velocity, and Dead Stock (on-hand but unmoved in 30 days, ranked by tied-up value).
  - **Stock-Out Audit**: pulls all shipped-out Pancake orders for a range, explodes them through unit-code recipes, and compares against what was actually released through the Stocks module — per-product variance with Matched / kulang / sobra badges plus unmapped order-item names so coverage gaps are visible. ⚠️ **Audit only — the Pancake side never deducts.** Auto-deducting here would double-count against the manual updates.
- **Placeholders:** Logistics Settings (ComingSoon). PO Stock-Out remains a stub (wire to Stocks releases next).

> ⚠️ **Sidebar gotcha:** a `NavItem` **with `children` only expands on click — it never navigates to its own `href`.** Any parent route that has a real page (`/logistics/warehouse`, `/logistics/inventory`) needs an explicit child link pointing back at itself, or the page is unreachable. Those child hrefs also become grantable in the User Management permission matrix, which builds its checkboxes from child hrefs.

## Problem Cost Solution — Problem Management / RCA (`/business/board`, migration `0014`)

Replaced the `BOARD` section, whose "Kanban Board" link had **never had a page**. Built from scratch: tracks an issue from identification to resolution with an owner, root cause, solution, deadline and status.

- **Data:** `problems` (auto `PRB-0001` codes, the RCA chain `cause → root_cause → solution → action_plan → corrective_action → preventive_action`, target/actual dates, evidence, approval), `problem_departments` (**`manager_emails[]` is what drives the Manager role**), `problem_comments` (threaded via `parent_id`, `mentions[]`, attachments), `problem_activity` (timeline), `problem_notifications` (email queue with a partial-unique `dedupe_key`), plus the `problem-files` storage bucket.
- **`src/lib/problems-store.ts`** — `useProblems()`, `useProblemDepartments()` (seeds 15 departments on first load), `useProblemThread(id)`. Helpers: `deadlineInfo()` → 🟢 on-track / 🟡 due-soon / 🔴 overdue, `completionBlockers()` (Root Cause + Solution + Completion Notes are required to close), `resolveRole()` / `managedDepartments()` / `canEdit()`, `uploadProblemFile()`, `enqueueNotification()`.
- **Roles:** Admin = `isMotherAccount()` · Manager = listed in a department's `manager_emails` · Employee = everyone else. Visibility is filtered by role; **employees "Submit for Approval", managers "Approve & Complete"**.
- **Pages:** `/business/board` (Dashboard tab = 7 stat cards + 6 charts; Problems tab = search + department/user/status/priority filters + a date range on reported|deadline|completed + quick scopes) and `/business/board/settings` (departments CRUD + manager assignment, admin-only). Components in `src/components/business/problems/`.
- ⚠️ `enqueueNotification` **pre-filters existing `dedupe_key`s** before inserting — a raw insert would hit `23505` and drop the whole batch.
- ⚠️ The sidebar label is **"Problem Cost Solution"** (the user's wording; possibly meant "Cause", never confirmed). The route stays `/business/board`.

## User Management (`/business/users`, LHIKE USER-MANUAL-User-Management)

`src/lib/users-store.ts` (`pesowise_users`, functional updates) + `users/page.tsx`. Roster table (LHIKE format, restyled per user): **ID / Username / Full Name / Position / Last Login / Action** — sortable headers (▲▼⇅, `sort` state, numeric-aware localeCompare), username = blue UNDERLINED link → edit (w/ PENDING/REQUESTED/MOTHER badges), Action = **Change Role** (teal) + **Enabled/Disabled** (blue toggle-look button; enabling clears pending). Toolbar = bordered bar: records select left, "Search:" right. Header buttons: small blue "Add User" + "Add Company Logo". Remove user = red button INSIDE the edit screen (`onRemove` → back to list + confirm modal). **Add New User** → starts PENDING + disabled (manual flow: user requests access sa login page → admin enables). **Edit user = LHIKE "Edit user information" layout**: Employee No. (readonly gray, = U-n id) + Username (kept — login match) + Last/First/Middle Name full-width, then two-column grid (Birthdate | Contact Number, Personal Email | **Company Email** [= the `email` field used for access matching], Gender Male/Female radios | Position, Employment Date | Employment Status Active/Inactive/Resigned/Terminated), ✓Save + Cancel right-aligned, Remove user left. `full_name` auto-derived = First + Last on save (table + matching still work). ErpUser gained: last_name/first_name/middle_name/birthdate/personal_email/gender/employment_date. **CHANGE ROLE = LHIKE "USER ROLE" layout**: profile LINES (Name : / Employee No : / Position : / Status : green-when-Active) + bordered panel PER SECTION w/ teal `titleCase` title (catalog = exported `businessNav` — auto-includes our extra sections na wala sa LHIKE: Ads/Sales Channels/Affiliates/Reports) + **bold module checkbox** (parent — checking it toggles ALL its children hrefs) w/ indented sub-checkboxes (children pages, o "View / Access" mirror para sa walang-children) + **Mother Account panel** sa baba ("Mother Account" + indented "Access for Mother Account", parehong bound sa `mother`; matrix grays out) + Submit/Cancel bottom-LEFT. **COMPANY LOGO** screen: jpg/png/jpeg → 480px PNG data URL sa `pesowise_company_logo`; current-vs-preview panes; **shows on the login page**. Login page also has "Request access" (matches email/username sa roster → sets `requested`). **ENFORCEMENT**: Sidebar filters `businessNav` per `accessFor()` — matched user (username/full_name vs `currentUserName()`, o email) → allowed hrefs lang (+dashboard); Mother o WALANG roster match → full nav (never locks out the owner); disabled/non-Active → dashboard lang. ⚠ localStorage = per-browser: cross-device enforcement + totoong request-access flow ay aandar pag Supabase persistence na. ⚠ **NEVER define a field-wrapper component INSIDE a form component** — remounts inputs per keystroke = focus loss (caught live sa Add User; hoisted `FormRowU` / `ViewOrderRow` / `ViewRtsRow` to module scope).

## Persistence — Supabase is the source of truth (localStorage is a read cache)

**This section used to say "no Supabase wiring yet". That is obsolete.** Migrations `0001`–`0016` live in `supabase/migrations/` and every module now reads/writes Supabase; `localStorage` (`pesowise_*`) survives only as a **same-session read cache** for instant paint, migrated up once on first load. Store shape: `use client` hook → paint from cache → fetch from Supabase (`.eq("business_id", …)`) → `normalize()` legacy/partial rows defensively → write through on every mutation.

- Single-business deployment: `getBusinessId()` = `businesses.select("id").limit(1)` — it is `"use client"` and **NOT usable server-side**. In scripts/API routes use `createSupabaseServerClient()` (service role) + the same one-liner.
- RLS on every table: `owner_id = auth.uid()` OR `public.is_business_member(business_id)`. Copy an existing policy verbatim when adding a table.
- **Migrations are run by the USER** in the Supabase SQL Editor — hand them the SQL (clipboard via PowerShell `Set-Clipboard` is the established flow) and wait for confirmation before assuming a table exists.
- ⚠️ **Check for name collisions before naming a table.** `subscriptions` was already taken by a Supabase starter template, which failed the whole migration with `42703: column "business_id" does not exist` — the `create table if not exists` silently skipped, then the index/policy hit the pre-existing table. Prefix when in doubt (`finance_subscriptions`).
- Supabase **Storage** is used for the first time by Problem Management (`problem-files` bucket, 50MB/file). Everything else still stores images as downscaled data URLs.

The Claude Code **preview panel has isolated localStorage** and its **auth cookie expires** (~1h → redirects to `/login`), so live UI usually can't be driven from here — verify with `tsc --noEmit` + `npm run build` + Node scripts against Supabase, and ask the user to confirm in their authenticated browser.

## Shared client libs (use these — do not re-implement per page)

- **`src/lib/pancake-cache.ts`** — `cachedJson(url, { force })`. Module-level 15-min response cache that **survives client-side navigation and clears on hard refresh**, plus in-flight de-duplication. This is what stops the "every tab re-loads with a spinner" complaint: first visit fetches, revisits are instant, the Refresh button passes `force: true`. **Every Pancake fetch in the app routes through this** (warehouse dashboard, PPW, shipped-out, inventory dashboard, RTS, fulfillment, sales, business dashboard, adspent, `courier-live`). Do not add a raw `fetch("/api/pancake/…")`.
- **`src/lib/courier.ts`** — `courierOf(name, tracking)`, `courierTally()`, `COURIERS`, `COURIER_COLOR`. **We ship SPX and J&T only.** Resolution is name-first, then **tracking prefix** — this fallback is load-bearing: parcels that have a printed waybill but haven't been collected often come back with an EMPTY `partner.partner_name`, which used to dump hundreds of SPX parcels into "OTHER". Verified live: `SPEPH…` = SPX, `JT…` and long numerics (`980208157812`) = J&T. Four divergent local `courierLabel` copies were replaced by this — don't reintroduce one.
- **`src/components/business/Skeleton.tsx`** — `Skeleton`, `StatCardSkeleton`, `StatCardsSkeleton`, `TableSkeleton`, `ChartSkeleton`. Use while loading instead of rendering zeros (a card reading "0" is indistinguishable from real empty data).

## Scripts + scheduled tasks (`scripts/`, headless, service-role)

All are plain `.mjs` run by Node with no build step. They parse `../.env.local` for `NEXT_PUBLIC_SUPABASE_URL` / `SUPABASE_SERVICE_ROLE_KEY`, and **fall back to `process.env`** when that file is absent (CI/cloud). Driven by **Claude Code scheduled tasks**, which only fire while the Claude Code app is open.

- **`roas-discord-report.mjs`** — Page ROAS report → Discord as a **PNG image** (HTML replica → headless Chrome `--screenshot` → `sharp.trim()` → multipart upload). `--window today|yesterday|mtd`, `--dry-run` (writes `scripts/_roas-report-preview.png`), `--no-fb`, `--text`, `--force`, `--slots`.
  - ⚠️ Chrome's `--screenshot` is only as large as `--window-size`, so a wide table **silently crops**. The layout is measured first (`--dump-dom` + a `data-size` attribute) and the viewport sized to it.
  - ⚠️ Runs **snap to their scheduled slot** (`SLOTS_MIN` = 9/12/3/6/9PM + 11:50PM). A delayed run still stamps "9:00PM edition (data as of 9:55PM PHT)", and a post-midnight EOD run closes the day it was chasing. **Keep `SLOTS_MIN` in sync with the cron.**
  - ⚠️ Dedup stamp `scripts/_roas-last-sent` (90 min) stops catch-up double-sends when several missed slots fire at once. `--force` bypasses it and does **not** write the stamp.
  - Config: `DISCORD_WEBHOOK_URL` in `.env.local` (webhook username "AI Agent Report"). ⚠️ `vercel env pull` overwrites `.env.local` and drops it — re-add.
- **`subscriptions-bill.mjs`** — posts due `finance_subscriptions` to Book Keeping. `--dry-run`, `--date=YYYY-MM-DD`.
- **`problem-notify.mjs`** — queues Problem Management deadline reminders (7/3/1-day, due-today, daily-overdue) and sends the queue via **Resend**. Without `RESEND_API_KEY` the queue simply accumulates — that is not an error.
- **`fb-accounts.mjs` / `fb-charges-import.mjs`** — the FB Billing daily scrape (browser-driven, must stay local).

> **GitHub Actions was tried for scheduling and reverted.** Free cron ran **1h52m–2h35m late and skipped runs**; moving off `:00` to off-peak minutes only got it to ~73 min. ROAS reporting is back on local scheduled tasks. Anything needing the user's logged-in Chrome (FB Billing scrape, Gmail receipts) can never run in the cloud.

> **Dependency:** `xlsx-js-style` (SheetJS fork w/ cell styling) is used client-side for Book Keeping upload + finance/product exports. Based on SheetJS 0.18 (low-severity advisories) — fine for the user's own files; move server-side/exceljs if untrusted uploads ever matter.
